#pragma once
#include "include.h"

template <typename T1, typename T2>
class RenamedGraphManager
{
public:
    RenamedGraphManager(uint32 buffer_size);
    RenamedGraphManager();
    ~RenamedGraphManager();
};

